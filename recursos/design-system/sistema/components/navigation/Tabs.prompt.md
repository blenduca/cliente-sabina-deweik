Tab switcher. `line` (accent underline) for page/section nav; `pill` (amarelo filled) for compact in-card filters.

```jsx
<Tabs tabs={["Hoje","Agenda","Insights","Bastidores"]} variant="line" />
<Tabs tabs={["Todos","IA","Cultura"]} variant="pill" />
```
