Dropdown menu — trigger button + popover list. Closes on outside click. Items can be `{divider:true}` or `{danger:true}`.

```jsx
<Menu label="Conta" items={["Perfil","Preferências",{divider:true},{label:"Sair",danger:true}]} align="right" />
```
